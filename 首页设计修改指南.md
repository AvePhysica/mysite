# 首页设计修改指南

这份文档说明当前 VuePress + Plume 首页的组成方式，以及如何在不破坏文档页面和导航栏的前提下自行调整首页。

## 一、首页由哪些文件组成

当前首页主要涉及以下文件：

```text
D:\VuePress_mysite
├── docs
│   ├── README.md
│   └── .vuepress
│       ├── config.js
│       ├── client.js
│       ├── components
│       │   └── HomeLanding.vue
│       └── styles
│           └── index.scss
├── tests
│   └── homepage-smoke.mjs
└── package.json
```

各文件的职责如下：

| 文件 | 作用 |
| --- | --- |
| `docs/README.md` | 声明这是首页，并把自定义首页组件放入 Plume 首页布局 |
| `docs/.vuepress/client.js` | 向 VuePress 注册 `HomeLanding` 组件并加载全局样式 |
| `docs/.vuepress/components/HomeLanding.vue` | 首页的内容、自动统计逻辑和主要视觉样式 |
| `docs/.vuepress/styles/index.scss` | 修改 Plume 首页容器、导航栏和页脚的全局样式 |
| `docs/.vuepress/config.js` | 配置 Plume，并把文档标题、日期、标签等信息提供给首页 |
| `tests/homepage-smoke.mjs` | 检查构建后的首页是否包含必要内容和链接 |

不要直接修改以下目录：

```text
docs/.vuepress/.cache
docs/.vuepress/.temp
docs/.vuepress/dist
docs/.vuepress/.verify-cache
docs/.vuepress/.verify-temp
docs/.vuepress/.verify-dist
node_modules
```

这些都是 VuePress、测试构建或依赖安装自动生成的文件。

## 二、首页入口：`docs/README.md`

当前首页入口大致如下：

```md
---
home: true
icon: house
title: RHJ Blog & Notes
description: Physics · AI · Mathematics · Learning
config:
  - type: custom
    full: true
---

<HomeLanding />
```

这里最重要的是：

- `home: true`：告诉 Plume 当前页面是首页。
- `type: custom`：不使用默认 Hero，而是使用自定义内容。
- `full: true`：让首页主体至少占满一屏。
- `<HomeLanding />`：插入自定义首页组件。

如果想修改浏览器标签页显示的首页标题，可以调整：

```yaml
title: RHJ Blog & Notes
```

一般不需要在这个文件中继续添加复杂 HTML。首页内容集中放在 `HomeLanding.vue` 中更容易维护。

## 三、组件注册：`docs/.vuepress/client.js`

这个文件负责让 Markdown 能识别 `<HomeLanding />`：

```js
import { defineClientConfig } from "vuepress/client";
import HomeLanding from "./components/HomeLanding.vue";
import "./styles/index.scss";

export default defineClientConfig({
  enhance({ app }) {
    app.component("HomeLanding", HomeLanding);
  },
});
```

如果以后新建另一个 Vue 组件，例如：

```text
docs/.vuepress/components/HomeQuote.vue
```

可以这样注册：

```js
import HomeQuote from "./components/HomeQuote.vue";

export default defineClientConfig({
  enhance({ app }) {
    app.component("HomeLanding", HomeLanding);
    app.component("HomeQuote", HomeQuote);
  },
});
```

然后便可以在 Markdown 或其他 Vue 组件中使用：

```md
<HomeQuote />
```

## 四、首页主体：`HomeLanding.vue`

这个文件分为三个部分：

```vue
<script setup>
// 数据和自动统计逻辑
</script>

<template>
  <!-- 首页显示的 HTML 结构 -->
</template>

<style scoped>
/* 只作用于当前组件的样式 */
</style>
```

### 1. 修改首页文字

在 `<template>` 中找到：

```vue
<p class="home-kicker">RHJ's Notes</p>
<h1 id="home-title">Blog &amp; Notes</h1>
<p class="home-intro">
  Exploring physics, mathematics, artificial intelligence and the
  ideas connecting them.
</p>
```

可以直接修改为自己的内容，例如：

```vue
<p class="home-kicker">RHJ's Knowledge Base</p>
<h1 id="home-title">Physics &amp; Research</h1>
<p class="home-intro">
  记录物理、数学、人工智能与科研学习过程。
</p>
```

注意：在 HTML 中单独显示 `&` 时，建议写成：

```html
&amp;
```

### 2. 修改首页入口按钮

首页三个圆角按钮位于：

```vue
<nav class="home-actions" aria-label="主要内容入口">
  <RouteLink to="/physics/">Physics</RouteLink>
  <RouteLink to="/japanese/">Japanese</RouteLink>
  <RouteLink to="/computer-science/">Computer Science</RouteLink>
</nav>
```

例如增加一个 About 按钮：

```vue
<RouteLink to="/about/">About</RouteLink>
```

对应的 `docs/about/README.md` 必须存在，否则点击后会进入 404 页面。

### 3. 修改统计项目

统计卡片来自 `<script setup>` 中的 `sections`：

```js
const sections = [
  { label: "Physics", path: "/physics/", prefix: "/physics/" },
  {
    label: "QFT Lectures",
    path: "/physics/",
    prefix: "/physics/Quantum%20Field%20Theory/",
  },
  { label: "Japanese", path: "/japanese/", prefix: "/japanese/" },
  {
    label: "Computer Science",
    path: "/computer-science/",
    prefix: "/computer-science/",
  },
];
```

每一项包含：

- `label`：首页显示的名称。
- `path`：点击统计卡片后前往的页面。
- `prefix`：统计哪些网址下的文档。

QFT 文件夹名中存在空格，因此 URL 中的空格写成 `%20`：

```text
文件夹：Quantum Field Theory
URL：Quantum%20Field%20Theory
```

除非同时修改文件夹和所有相关链接，否则不要随意把这一段改成其他拼写。

如果增加 AI 文件夹：

```text
docs/ai/README.md
```

可以在数组中增加：

```js
{
  label: "Artificial Intelligence",
  path: "/ai/",
  prefix: "/ai/",
},
```

同时还要在 `config.js` 中增加对应的导航栏和侧边栏配置。

### 4. 修改最近更新数量

最近更新目前显示四篇文档：

```js
.slice(0, 4)
```

修改数字即可改变数量：

```js
.slice(0, 6)
```

桌面端每行显示两张卡片，手机端每行显示一张。

### 5. 修改文档分类规则

首页通过网址判断文档属于哪个分类：

```js
function categoryOf(path) {
  if (path.startsWith("/physics/Quantum%20Field%20Theory/")) return "QFT";
  if (path.startsWith("/physics/")) return "Physics";
  if (path.startsWith("/japanese/")) return "Japanese";
  return "Computer Science";
}
```

如果增加新目录，应在最后的默认分类之前增加判断。例如：

```js
if (path.startsWith("/ai/")) return "AI";
```

同时在 `fallbackDescription()` 中增加对应的默认简介：

```js
AI: "Machine learning, neural networks and large language models.",
```

## 五、最近更新如何自动生成

`config.js` 中的 `extendsPage()` 会在 VuePress处理每篇 Markdown 时收集以下信息：

- 页面标题
- 页面简介
- 标签
- 页面日期或文件最后修改时间

这些信息被放进 `page.routeMeta.homepage`，然后由 `HomeLanding.vue` 读取。

主要逻辑如下：

```js
extendsPage(page) {
  const frontmatterDate = page.frontmatter.date;
  let updatedAt = frontmatterDate
    ? new Date(frontmatterDate).getTime()
    : 0;

  // 没有填写 date 时，使用文件最后修改时间

  page.routeMeta.homepage = {
    title: page.title,
    description: page.frontmatter.description ?? "",
    tags: page.frontmatter.tags ?? [],
    updatedAt,
  };
}
```

首页按照 `updatedAt` 从新到旧排序。

### 推荐的 Markdown 头部格式

为了让最近更新卡片显示更准确，可以给文档添加：

```yaml
---
title: Coleman QFT Lecture 1：在量子力学中引入相对论
date: 2026-08-03
description: 介绍为什么相对论与量子力学共同要求使用量子场论。
tags:
  - QFT
  - Relativity
---
```

字段说明：

| 字段 | 作用 |
| --- | --- |
| `title` | 页面标题和首页卡片标题 |
| `date` | 最近更新时间，建议使用 `YYYY-MM-DD` |
| `description` | 首页卡片摘要 |
| `tags` | 首页卡片标签，目前最多显示前两个 |

如果不填写 `date`，首页使用文件最后修改时间。

如果不填写 `description`，首页使用分类对应的默认英文简介。

## 六、修改背景和颜色

主要视觉样式位于 `HomeLanding.vue` 的 `<style scoped>` 中。

### 1. 页面背景色

找到 `.home-dashboard`：

```css
.home-dashboard {
  background:
    radial-gradient(...),
    linear-gradient(...),
    #202326;
}
```

最后的 `#202326` 是基础背景色。例如改得更蓝：

```css
#17212b
```

改得更黑：

```css
#151719
```

### 2. 网格大小

当前网格间距是 32 像素：

```css
background-size: auto, auto, 32px 32px, 32px 32px, auto;
```

更密：

```css
24px 24px
```

更疏：

```css
48px 48px
```

修改时需要同时调整两个 `32px 32px`。

### 3. 渐变标题颜色

找到 `.home-kicker`：

```css
background: linear-gradient(
  100deg,
  #8ed9ef 4%,
  #b5d6c6 48%,
  #eedc9b 92%
);
```

三个颜色分别控制渐变的左侧、中间和右侧。

### 4. 按钮颜色

普通按钮：

```css
.home-actions a {
  background: rgb(72 84 102 / 66%);
}
```

第一个按钮和鼠标悬停状态：

```css
.home-actions a:first-child,
.home-actions a:hover {
  color: #10252b;
  background: #89cce1;
}
```

如果不希望第一个按钮始终高亮，删除：

```css
.home-actions a:first-child,
```

只保留：

```css
.home-actions a:hover
```

### 5. 分别调整亮色和深色模式

深色模式是 `HomeLanding.vue` 中组件样式的默认状态。

亮色模式覆盖规则集中放在：

```text
docs/.vuepress/styles/index.scss
```

选择器形式如下：

```scss
html[data-theme="light"] .home-dashboard {
  /* 亮色首页背景 */
}

html[data-theme="light"] .update-panel {
  /* 亮色 Recent Update 面板 */
}

html[data-theme="light"] .note-card {
  /* 亮色文档卡片 */
}
```

导航栏和页脚也有单独的亮色规则：

```scss
html[data-theme="light"] body:has(.home-dashboard) .vp-navbar {
  /* 亮色首页导航栏 */
}

html[data-theme="light"] body:has(.home-dashboard) .vp-footer {
  /* 亮色首页页脚 */
}
```

如果修改了某个深色元素，通常也应检查 `index.scss` 中是否需要同步修改对应的亮色版本。

不要把这种祖先级主题选择器放进 `<style scoped>` 并只给 `html` 使用 `:global()`；Vue 的 scoped 样式编译可能会丢失后面的首页选择器。祖先级主题规则放在 `index.scss` 最可靠。

## 七、调整毛玻璃面板

Recent Update 面板由 `.update-panel` 控制：

```css
.update-panel {
  background: ...;
  border: 1px solid rgb(255 255 255 / 13%);
  border-radius: 18px;
  box-shadow: ...;
  backdrop-filter: blur(18px);
}
```

常见调整：

```css
/* 模糊更强 */
backdrop-filter: blur(28px);

/* 圆角更大 */
border-radius: 28px;

/* 边框更明显 */
border: 1px solid rgb(255 255 255 / 24%);
```

文档卡片由 `.note-card` 控制，统计卡片由 `.stat-card` 控制。

## 八、调整宽度和间距

首页内容最大宽度：

```css
.home-shell {
  width: min(1180px, calc(100% - 48px));
}
```

例如希望页面更宽：

```css
width: min(1320px, calc(100% - 48px));
```

Hero 与 Recent Update 之间的距离：

```css
.home-hero {
  margin-bottom: clamp(64px, 8vw, 96px);
}
```

Recent Update 内边距：

```css
.update-panel {
  padding: clamp(24px, 4vw, 40px);
}
```

## 九、响应式布局

首页使用两个主要断点：

```css
@media (max-width: 900px) {
  /* 统计区域从四列变成两列 */
}

@media (max-width: 720px) {
  /* 最近更新从两列变成一列 */
}

@media (max-width: 480px) {
  /* 手机小屏进一步缩小间距和按钮 */
}
```

如果卡片在平板上显得太窄，可以把：

```css
@media (max-width: 720px)
```

改为：

```css
@media (max-width: 800px)
```

这样更早切换成单列。

## 十、全局样式：`styles/index.scss`

这个文件主要解决三件事：

1. 去掉 Plume 自定义首页容器的默认内边距。
2. 让自定义首页真正铺满可用宽度。
3. 首页显示时，让导航栏和页脚与深色背景协调。

例如：

```scss
.vp-home-custom {
  padding: 0 !important;
}
```

导航栏使用：

```scss
body:has(.home-dashboard) .vp-navbar {
  background: rgb(27 30 32 / 78%) !important;
  backdrop-filter: blur(14px);
}
```

`body:has(.home-dashboard)` 表示这些样式只在首页存在时生效，不会影响普通文档页。

如果希望导航栏更透明，可以降低 `78%`；如果希望更实，可以提高到 `90%`。

## 十一、导航栏仍在 `config.js` 中修改

顶部导航栏不是在 `HomeLanding.vue` 中配置的，而是在：

```text
docs/.vuepress/config.js
```

例如：

```js
navbar: [
  { text: "首页", link: "/" },
  { text: "物理", link: "/physics/" },
  { text: "日本語", link: "/japanese/" },
  { text: "计算机科学", link: "/computer-science/" },
],
```

顶部左侧站点名称由以下配置控制：

```js
title: "Luminosity",
```

修改为：

```js
title: "RHJ Notes",
```

即可改变导航栏左侧名称以及部分页面的标题后缀。

## 十二、修改后的预览与检查

启动开发预览：

```powershell
npm run docs:dev
```

访问：

```text
http://localhost:8081
```

如果修改后页面没有变化：

1. 在浏览器中按 `Ctrl + F5`。
2. 检查终端是否出现红色错误。
3. 停止开发服务器后重新运行。

完整构建：

```powershell
npm run docs:build
```

首页结构检查：

```powershell
npm run test:homepage
```

如果开发服务器正在运行，默认缓存目录可能被占用。此时可以先停止开发服务器，再执行完整构建；或者使用独立验证目录：

```powershell
.\node_modules\.bin\vuepress.cmd build docs `
  --dest docs\.vuepress\.verify-dist `
  --temp docs\.vuepress\.verify-temp `
  --cache docs\.vuepress\.verify-cache `
  --clean-temp `
  --clean-cache
```

## 十三、常见问题

### 首页显示 `<HomeLanding />` 文字而不是组件

检查：

- `client.js` 是否存在。
- 是否正确导入 `HomeLanding.vue`。
- 注册名是否仍然是 `HomeLanding`。
- `README.md` 中的组件名大小写是否完全一致。

### 最近更新没有出现某篇文章

检查文章是否位于以下路径之一：

```text
docs/physics
docs/japanese
docs/computer-science
```

首页目前只收集这三个区域中的页面。

### 最近更新时间不符合预期

在 Markdown frontmatter 中明确填写：

```yaml
date: 2026-08-03
```

填写后，首页会优先使用这个日期，而不是文件系统修改时间。

### 首页卡片没有摘要

添加：

```yaml
description: 这里填写适合显示在首页的一到两句话。
```

### 修改后出现中文乱码

所有 Markdown、Vue、JavaScript 和 SCSS 文件都应保存为 UTF-8。

不要使用会把整个文件转换成系统默认编码的批量替换方式。标题建议直接写为普通字符串，例如：

```yaml
title: Coleman QFT Lecture 4：场的正则量子化
```

而不是让脚本根据复杂对象自动推导标题。

## 十四、推荐的调整顺序

第一次自行调整时，建议按这个顺序进行：

1. 修改 Hero 标题、简介和按钮文字。
2. 修改背景颜色与渐变标题颜色。
3. 调整 Recent Update 的透明度和圆角。
4. 给重要 Markdown 添加 `date`、`description` 和 `tags`。
5. 最后再调整宽度、间距和手机端断点。

每完成一类修改就刷新浏览器检查一次，这样出现问题时更容易定位是哪一处改动造成的。
